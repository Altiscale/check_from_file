require 'spec_helper'

describe CheckFromFile do
  it 'has a version number' do
    expect(CheckFromFile::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
